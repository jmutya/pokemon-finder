<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-gray-300 text-white font-mono flex justify-center items-center min-h-screen">
    <div class="bg-red-500 rounded-lg shadow-xl p-6 max-w-md border-4 border-black relative">
      <!-- Pokedex Header -->
      <div class="flex items-center justify-between mb-5">
        <div class="w-10 h-10 bg-blue-500 rounded-full border-4 border-white"></div>
        <div class="flex space-x-2">
          <div class="w-4 h-4 bg-yellow-400 rounded-full"></div>
          <div class="w-4 h-4 bg-green-400 rounded-full"></div>
          <div class="w-4 h-4 bg-red-400 rounded-full"></div>
        </div>
      </div>
      
      <!-- Pokemon Information -->
      <h1 class="text-3xl font-bold text-center uppercase border-b-2 pb-2 border-black">
        <%= pokemon.name %>
      </h1>
      <div class="flex flex-col items-center mt-4 bg-gray-200 p-4 rounded-lg shadow-inner text-black">
        <img
          src="<%= pokemon.sprites.front_default %>"
          alt="<%= pokemon.name %>"
          class="w-40 h-40 border-2 border-black bg-white rounded-lg"
        />
        <p class="text-lg font-semibold mt-2">Height: <%= pokemon.height %></p>
        <p class="text-lg font-semibold">Weight: <%= pokemon.weight %></p>
        <p class="text-lg font-semibold">
          Type: <%= pokemon.types.map(t => t.type.name).join(', ') %>
        </p>
      </div>
      
      <!-- Back Button -->
      <div class="text-center mt-5">
        <a href="/" class="px-4 py-2 bg-yellow-500 text-black font-bold rounded-lg shadow-lg border-2 border-black">Back</a>
      </div>
    </div>
  </body>
</html>
